# ✅ Задачи · Уровень 3 — Геймплей

> Здесь симулятор становится настоящей игрой: интерфейс, клиент-сервер, сохранение, прогрессия.

---

## Движение (модуль 13)

1. Дверь: телепорт по CFrame → переделай в плавный TweenService.
2. Платформа-лифт (Tween вверх-вниз).
3. ⭐ Зона-ускоритель: WalkSpeed 50 при входе, 16 при выходе.

## GUI (модуль 14)

4. ScreenGui со счётчиком (TextLabel, Scale). Frame-магазин с кнопками (UIListLayout, UICorner).
5. LocalScript: реакция на `MouseButton1Click`, обновление текста счётчика.
6. ⭐ Плавный показ/скрытие магазина (TweenService).

## Клиент-сервер (модуль 15)

7. RemoteEvent `BuyUpgrade`: клиент `FireServer`, сервер `OnServerEvent` печатает игрока и тип.
8. Серверная проверка валюты (хватает → списать, нет → отказ).
9. ⭐ Сервер обновляет GUI клиента через `FireClient` после покупки.
10. ⭐⭐ Объясни, почему `player` в `OnServerEvent` нельзя подделать.

## Сохранение (модуль 16)

11. Включи API-доступ. Сохраняй/загружай валюту (DataStore, `pcall`, ключ по UserId).
12. Проверь цикл: набрал → вышел → зашёл → валюта на месте.
13. ⭐ Сохраняй таблицу (валюта + апгрейд), `BindToClose`, автосейв.

## Механики (модуль 17)

14. `leaderstats` с `Coins` (виден в Tab). Свяжи сбор с ростом числа.
15. Апгрейд скорости через сервер (рост цены, эффект). ⭐ Сохрани уровень, переприменяй после респауна.

---

## 🎯 Итоговое

16. Собери полную петлю: собрал монеты → видно в leaderstats и GUI → купил апгрейд (сервер проверил) →
    собираешь быстрее → всё сохраняется между сессиями. Это играбельный симулятор.

➡️ Проект уровня: [PROJECT.md](PROJECT.md)
