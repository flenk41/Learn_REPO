# ✅ Задачи · Уровень 2 — Скриптинг (ядро)

> Это ядро трека. Пиши код, запускай ▶ Play, читай Output. Ошибки — это нормально, читай их текст.

---

## Luau (модуль 08)

1. Script в ServerScriptService: переменные, склейка строк, функция `addScore`. Вывод в Output.
2. Цикл `for` 1..10 и условие `if/elseif/else` по очкам.
3. ⭐ Таблица-список и таблица-словарь; перебери `ipairs`/`pairs`.

## Объекты из кода (модуль 09)

4. Скриптом измени цвет/материал платформы.
5. Создай монету `Instance.new` (Parent последним!). Удали её через `:Destroy` спустя время.
6. ⭐ Заготовка в ReplicatedStorage + наспавни 10 копий `:Clone()`.

## События (модуль 10)

7. Монета: `Touched` → проверка Humanoid → `print` + `:Destroy`.
8. Добавь debounce к кнопке-Part.
9. ⭐ «Лава»: урон по Humanoid.Health при касании, с debounce.
10. Подпишись на `PlayerAdded` — печатай имя (проверь в Start с 2 игроками).

## Сервисы и типы скриптов (модуль 11)

11. Script vs LocalScript: где печатается каждый (режим Start)?
12. ModuleScript `GameUtils` + `require` из серверного скрипта.
13. ⭐⭐ Объясни на примере очков: почему начисление — на сервере, а не на клиенте?

## Состояние и цикл (модуль 12)

14. Обратный отсчёт через `task.wait`. Цикл раундов с фазами.
15. ⭐ Вращай платформу через `Heartbeat` с deltaTime.

---

## 🎯 Итоговое

16. Оживи зону сбора: монеты, которые при касании игроком исчезают и (через таблицу) добавляют счёт
    игроку на сервере. Это ядро проекта уровня.

➡️ Проект уровня: [PROJECT.md](PROJECT.md)
